apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skywalking.fullname" . }}-test-connection"
  labels:
{{ include "skywalking.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "skywalking.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
